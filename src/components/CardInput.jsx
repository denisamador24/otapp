const CardInput = ({ children }) => {
  
  
  return (
    <div
      style={{
        "margin": "32px 8px",
        "padding": "8px",
        "borderRadius": "8px",
        "border": "2px solid skyblue",
      }}
    >
      {children}
    </div>
  )
}

export default CardInput;